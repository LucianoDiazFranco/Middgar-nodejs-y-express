<main class="reset-password-main">
  <div class="reset-password-container">
    <h1 class="reset-password-title">Restablecer tu contraseña</h1>
    {{#if error}}
      <p class="error-message">{{error}}</p>
    {{/if}}
    {{#if success}}
      <p class="success-message">{{success}}</p>
    {{/if}}
    <form action="/reset_password/{{token}}" method="POST" class="reset-password-form">
      <input type="password" name="password" placeholder="Nueva contraseña" class="input-field-reset-password" required>
      <input type="password" name="confirm_password" placeholder="Confirmar nueva contraseña" class="input-field-reset-password" required>
      <button type="submit" class="reset-password-button">Restablecer contraseña</button>
    </form>
  </div>
</main>
